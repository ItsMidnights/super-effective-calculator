import { StyleSheet } from "react-native";

export const pokemonStyle = StyleSheet.create({
  container: {
    alignItems: "center",
  },
  image: {
    width: 200,
    height: 200,
  },
  types: {
    flexDirection: "row",
  },
});
