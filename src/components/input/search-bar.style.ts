import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    maxWidth: 400,
    borderRadius: 25 
  },
  input: {
    backgroundColor: "#E1E8EE",
    borderRadius: 25,
  }
});